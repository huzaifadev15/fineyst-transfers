{%- liquid
  assign per_page = section.settings.products_per_page | default: 24
  assign cut_keys = "Adult,Women,Youth,Men,Girls,Infant" | split: ","
-%}

<section class="main-collection-tshirts">
  <div class="container coll-head">
    <div class="coll-head-text">
      <h1>{{ collection.title }}</h1>
      {%- if collection.description != blank -%}
        <div class="coll-desc rte">{{ collection.description }}</div>
      {%- endif -%}
    </div>
    {%- assign badge_blocks = section.blocks | where: 'type', 'badge' -%}
    {%- if badge_blocks.size > 0 -%}
      <div class="coll-badges">
        {%- for badge in section.blocks -%}
          {%- if badge.type == 'badge' -%}
            <div class="coll-badge" {{ badge.shopify_attributes }}>
              <span class="coll-badge-icon">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M13 2 3 14h7l-1 8 10-12h-7z"/></svg>
              </span>
              <span>{{ badge.settings.text }}</span>
            </div>
          {%- endif -%}
        {%- endfor -%}
      </div>
    {%- endif -%}
  </div>

  {%- if section.settings.show_quick_nav and section.blocks.size > 0 -%}
    <div class="coll-quicknav-wrap">
      <div class="container">
        <div class="coll-quicknav">
          {%- for block in section.blocks -%}
            {%- if block.type == 'quick_link' -%}
              <a href="{{ block.settings.link }}" class="coll-quicktab{% if block.settings.link contains collection.handle %} is-active{% endif %}" {{ block.shopify_attributes }}>
                <span class="coll-quicktab-thumb">
                  {%- if block.settings.image -%}
                    {{ block.settings.image | image_url: width: 64 | image_tag: loading: 'lazy', alt: block.settings.label }}
                  {%- else -%}
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M9 3h6l2 3-3 2v11H10V8L7 6l2-3z"/></svg>
                  {%- endif -%}
                </span>
                <span class="coll-quicktab-label">{{ block.settings.label }}</span>
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="coll-quicktab-chevron"><path d="m9 18 6-6-6-6"/></svg>
              </a>
            {%- endif -%}
          {%- endfor -%}
        </div>
      </div>
    </div>
  {%- endif -%}

  <div class="container coll-body">

    <aside class="coll-sidebar" id="collFilters">

      <div class="filter-group" data-filter-group>
        <button type="button" class="filter-group-hd" data-filter-toggle>
          Cut
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="filter-chevron"><path d="m6 9 6 6 6-6"/></svg>
        </button>
        <div class="filter-group-body">
          <ul class="filter-list">
            {%- assign cut_counts_str = "" -%}
            {%- for product in collection.products -%}
              {%- if product.title contains "Youth" -%}{%- assign this_cut = "Youth" -%}
              {%- elsif product.title contains "Toddler" or product.title contains "Infant" -%}{%- assign this_cut = "Infant" -%}
              {%- elsif product.title contains "Girls" -%}{%- assign this_cut = "Girls" -%}
              {%- elsif product.title contains "Women" -%}{%- assign this_cut = "Women" -%}
              {%- elsif product.title contains "Men's" -%}{%- assign this_cut = "Men" -%}
              {%- else -%}{%- assign this_cut = "Adult" -%}
              {%- endif -%}
              {%- assign cut_counts_str = cut_counts_str | append: '|' | append: this_cut | append: '|' -%}
            {%- endfor -%}
            {%- for key in cut_keys -%}
              {%- assign cut_needle = '|' | append: key | append: '|' -%}
              {%- assign cut_parts = cut_counts_str | split: cut_needle -%}
              {%- assign cut_count = cut_parts.size | minus: 1 -%}
              {%- if cut_count > 0 -%}
                <li>
                  <label class="filter-check">
                    <input type="checkbox" data-cut-filter value="{{ key }}">
                    <span>{{ key }}</span>
                    <small>({{ cut_count }})</small>
                  </label>
                </li>
              {%- endif -%}
            {%- endfor -%}
          </ul>
        </div>
      </div>

      {%- assign color_names_raw = "" | split: "" -%}
      {%- for product in collection.products -%}
        {%- assign pcolors = product.metafields.custom.swatch_colors.value -%}
        {%- for c in pcolors -%}
          {%- assign one_color_name = c.name | split: '§' -%}
          {%- assign color_names_raw = color_names_raw | concat: one_color_name -%}
        {%- endfor -%}
      {%- endfor -%}
      {%- assign color_names = color_names_raw | uniq -%}
      {%- if color_names.size > 0 -%}
        <div class="filter-group" data-filter-group>
          <button type="button" class="filter-group-hd" data-filter-toggle>
            Colors
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="filter-chevron"><path d="m6 9 6 6 6-6"/></svg>
          </button>
          <div class="filter-group-body">
            <div class="filter-swatch-grid">
              {%- for name in color_names limit: 24 -%}
                {%- assign swatch_hex = "#D9D9D9" -%}
                {%- for product in collection.products -%}
                  {%- assign pcolors = product.metafields.custom.swatch_colors.value -%}
                  {%- for c in pcolors -%}
                    {%- if c.name == name -%}{%- assign swatch_hex = c.hex -%}{%- break -%}{%- endif -%}
                  {%- endfor -%}
                {%- endfor -%}
                <button type="button" class="filter-swatch" data-color-filter data-color="{{ name | escape }}" style="background:{{ swatch_hex }};" title="{{ name | escape }}"></button>
              {%- endfor -%}
            </div>
          </div>
        </div>
      {%- endif -%}

      {%- if collection.filters.size > 0 -%}
        <form id="collFilterForm">
          {%- for filter in collection.filters -%}
            {%- if filter.values.size > 0 -%}
              <div class="filter-group" data-filter-group>
                <button type="button" class="filter-group-hd" data-filter-toggle>
                  {{ filter.label }}
                  <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="filter-chevron"><path d="m6 9 6 6 6-6"/></svg>
                </button>
                <div class="filter-group-body">
                  {%- if filter.type == 'price_range' -%}
                    <div class="filter-price-range">
                      <label>
                        <span>Min</span>
                        <input type="number" min="0" name="{{ filter.min_value.param_name }}" value="{{ filter.min_value.value }}" placeholder="{{ filter.range_min | money }}">
                      </label>
                      <label>
                        <span>Max</span>
                        <input type="number" min="0" name="{{ filter.max_value.param_name }}" value="{{ filter.max_value.value }}" placeholder="{{ filter.range_max | money }}">
                      </label>
                    </div>
                  {%- else -%}
                    <ul class="filter-list">
                      {%- for value in filter.values -%}
                        {%- if value.count > 0 or value.active -%}
                          <li>
                            <label class="filter-check">
                              <input type="checkbox" name="{{ value.param_name }}" value="{{ value.value }}" {% if value.active %}checked{% endif %}>
                              <span>{{ value.label }}</span>
                              <small>({{ value.count }})</small>
                            </label>
                          </li>
                        {%- endif -%}
                      {%- endfor -%}
                    </ul>
                  {%- endif -%}
                </div>
              </div>
            {%- endif -%}
          {%- endfor -%}
          <button type="submit" class="btn-blue filter-apply-btn">Apply Filters</button>
          {%- if collection.current_vendor or collection.current_type or current_tags.size > 0 -%}
            <a href="{{ collection.url }}" class="filter-clear-link">Clear all</a>
          {%- endif -%}
        </form>
      {%- endif -%}
    </aside>

    <div class="coll-main">
      <div class="coll-toolbar">
        <span class="coll-toolbar-note">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 7h11l3 4h4v6h-2M3 7v10h2m0 0a2 2 0 1 0 4 0m-4 0h10m0 0a2 2 0 1 0 4 0m-4 0h2"/></svg>
          {{ section.settings.delivery_note | default: 'Next-Day Delivery — Order by 5PM' }}
        </span>
        <span class="coll-count">Showing {{ collection.products_count }} product{% if collection.products_count != 1 %}s{% endif %}</span>
        <form id="collSortForm">
          <select name="sort_by" data-coll-sort>
            {%- for option in collection.sort_options -%}
              <option value="{{ option.value }}" {% if option.value == collection.sort_by %}selected{% endif %}>{{ option.name }}</option>
            {%- endfor -%}
          </select>
        </form>
      </div>

      {%- paginate collection.products by per_page -%}
        {%- if collection.products.size > 0 -%}
          <div class="product-grid" id="collProductGrid">
            {%- for product in collection.products -%}
              {%- assign p_cut = "" -%}
              {%- if product.title contains "Youth" -%}{%- assign p_cut = "Youth" -%}
              {%- elsif product.title contains "Toddler" or product.title contains "Infant" -%}{%- assign p_cut = "Infant" -%}
              {%- elsif product.title contains "Girls" -%}{%- assign p_cut = "Girls" -%}
              {%- elsif product.title contains "Women" -%}{%- assign p_cut = "Women" -%}
              {%- elsif product.title contains "Men's" -%}{%- assign p_cut = "Men" -%}
              {%- else -%}{%- assign p_cut = "Adult" -%}
              {%- endif -%}
              {%- assign p_colors = product.metafields.custom.swatch_colors.value -%}
              {%- assign p_color_names = "" -%}
              {%- for c in p_colors -%}{%- assign p_color_names = p_color_names | append: c.name | append: '|' -%}{%- endfor -%}

              <a class="product-card" href="{{ product.url }}" data-cut="{{ p_cut }}" data-colors="{{ p_color_names }}">
                <span class="product-card-media">
                  {%- if product.featured_image -%}
                    {{ product.featured_image | image_url: width: 500 | image_tag: loading: 'lazy', alt: product.title }}
                  {%- else -%}
                    <span class="hero-media-placeholder"></span>
                  {%- endif -%}
                </span>
                {%- if product.compare_at_price > product.price -%}
                  <span class="product-card-save">Save {{ product.compare_at_price | minus: product.price | times: 100.0 | divided_by: product.compare_at_price | round }}%</span>
                {%- endif -%}

                {%- if p_colors.size > 0 -%}
                  <span class="product-card-swatches">
                    {%- for c in p_colors limit: 8 -%}
                      <span class="pc-swatch" style="background:{{ c.hex }};"></span>
                    {%- endfor -%}
                    {%- if p_colors.size > 8 -%}<span class="pc-swatch-more">+{{ p_colors.size | minus: 8 }}</span>{%- endif -%}
                  </span>
                {%- endif -%}

                <span class="product-card-body">
                  <span class="product-card-price">
                    <small>As low as</small>
                    <strong>{{ product.price_min | money }}</strong>
                    {%- if product.compare_at_price > product.price -%}
                      <s>{{ product.compare_at_price | money }}</s>
                    {%- endif -%}
                  </span>
                  {%- if product.vendor != blank -%}<span class="product-card-vendor">{{ product.vendor }}</span>{%- endif -%}
                  <span class="product-card-title">{{ product.title }}</span>
                </span>
              </a>
            {%- endfor -%}
          </div>

          {%- if paginate.pages > 1 -%}
            <nav class="coll-pagination" aria-label="Pagination">
              {%- if paginate.previous -%}<a href="{{ paginate.previous.url }}" class="coll-page-link">&larr; Prev</a>{%- endif -%}
              {%- for part in paginate.parts -%}
                {%- if part.is_link -%}
                  <a href="{{ part.url }}" class="coll-page-link">{{ part.title }}</a>
                {%- else -%}
                  <span class="coll-page-link{% if part.title == paginate.current_page %} is-active{% endif %}">{{ part.title }}</span>
                {%- endif -%}
              {%- endfor -%}
              {%- if paginate.next -%}<a href="{{ paginate.next.url }}" class="coll-page-link">Next &rarr;</a>{%- endif -%}
            </nav>
          {%- endif -%}
        {%- else -%}
          <p class="coll-empty">No products found in this collection yet.</p>
        {%- endif -%}
      {%- endpaginate -%}
    </div>
  </div>
</section>

<style>
  .main-collection-tshirts { padding: 28px 0 60px; }
  .coll-head { display: flex; justify-content: space-between; align-items: flex-end; gap: 24px; flex-wrap: wrap; margin-bottom: 20px; }
  .coll-head h1 { font-size: 2rem; font-weight: 800; }
  .coll-desc { color: var(--pdp-muted); margin-top: 6px; max-width: 640px; font-size: 0.95rem; }
  .coll-badges { display: grid; grid-template-columns: repeat(2, 1fr); gap: 8px; }
  .coll-badge { display: flex; align-items: center; gap: 8px; padding: 10px 16px; border: 1px solid var(--pdp-line); border-radius: 10px; font-size: 0.85rem; font-weight: 700; white-space: nowrap; }
  .coll-badge-icon { color: var(--brand); display: flex; }

  .coll-quicknav-wrap { border-top: 1px solid var(--pdp-line); border-bottom: 1px solid var(--pdp-line); margin-bottom: 24px; background: #FAFBFC; }
  .coll-quicknav { display: flex; gap: 12px; overflow-x: auto; padding: 16px 0; }
  .coll-quicktab { display: inline-flex; align-items: center; gap: 10px; padding: 10px 16px; border: 1px solid var(--pdp-line); border-radius: 10px; font-size: 0.85rem; font-weight: 700; white-space: nowrap; flex-shrink: 0; background: #fff; transition: border-color .15s, box-shadow .15s; }
  .coll-quicktab:hover, .coll-quicktab.is-active { border-color: var(--brand); box-shadow: 0 2px 8px rgba(1,154,255,0.15); }
  .coll-quicktab-thumb { width: 26px; height: 26px; border-radius: 50%; overflow: hidden; display: flex; align-items: center; justify-content: center; background: var(--brand-tint); color: var(--brand); flex-shrink: 0; }
  .coll-quicktab-thumb img { width: 100%; height: 100%; object-fit: cover; }
  .coll-quicktab-label { flex: 1; }
  .coll-quicktab-chevron { color: var(--pdp-muted); flex-shrink: 0; }

  .coll-body { display: grid; grid-template-columns: 240px 1fr; gap: 32px; align-items: start; }
  .coll-sidebar { position: sticky; top: calc(var(--nav-bottom, 97px) + 20px); }
  .filter-group { border-bottom: 1px solid var(--pdp-line); padding: 14px 0; }
  .filter-group-hd { width: 100%; display: flex; justify-content: space-between; align-items: center; background: none; border: none; font-size: 0.9rem; font-weight: 700; cursor: pointer; padding: 0; }
  .filter-chevron { transition: transform .2s; }
  .filter-group[data-collapsed] .filter-chevron { transform: rotate(-90deg); }
  .filter-group[data-collapsed] .filter-group-body { display: none; }
  .filter-group-body { margin-top: 10px; display: flex; flex-direction: column; gap: 8px; max-height: 260px; overflow-y: auto; }
  .filter-check { display: flex; align-items: center; gap: 8px; font-size: 0.85rem; cursor: pointer; }
  .filter-check small { color: var(--pdp-muted); }
  .filter-price-range { display: flex; gap: 10px; }
  .filter-price-range label { display: flex; flex-direction: column; gap: 4px; font-size: 0.75rem; color: var(--pdp-muted); flex: 1; }
  .filter-price-range input { padding: 6px 8px; border: 1px solid var(--pdp-line); border-radius: 6px; width: 100%; }
  .filter-apply-btn { width: 100%; margin-top: 16px; padding: 10px; font-size: 0.85rem; }
  .filter-clear-link { display: block; text-align: center; margin-top: 10px; font-size: 0.8rem; color: var(--pdp-muted); text-decoration: underline; }
  .filter-swatch-grid { display: grid; grid-template-columns: repeat(6, 1fr); gap: 8px; }
  .filter-swatch { width: 26px; height: 26px; border-radius: 50%; border: 1.5px solid var(--pdp-line); cursor: pointer; padding: 0; transition: transform .1s, border-color .1s; }
  .filter-swatch:hover { transform: scale(1.12); }
  .filter-swatch.is-active { border-color: var(--brand); box-shadow: 0 0 0 2px var(--brand-tint); }

  .coll-toolbar { display: flex; justify-content: space-between; align-items: center; gap: 16px; margin-bottom: 18px; flex-wrap: wrap; }
  .coll-toolbar-note { display: flex; align-items: center; gap: 6px; font-size: 0.8rem; font-weight: 700; color: var(--brand-dark); }
  .coll-count { font-size: 0.85rem; color: var(--pdp-muted); margin-left: auto; }
  .coll-toolbar select { padding: 8px 12px; border: 1px solid var(--pdp-line); border-radius: 8px; font-size: 0.85rem; }

  .product-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; }
  .product-card { display: flex; flex-direction: column; border: 1px solid var(--pdp-line); border-radius: 12px; overflow: hidden; position: relative; transition: box-shadow .15s, transform .15s; }
  .product-card:hover { box-shadow: 0 8px 24px rgba(16,24,40,0.1); transform: translateY(-2px); }
  .product-card.is-filtered-out { display: none; }
  .product-card-media { display: block; aspect-ratio: 1; background: #f6f7f9; }
  .product-card-media img { width: 100%; height: 100%; object-fit: cover; }
  .product-card-save { position: absolute; top: 10px; left: 10px; background: var(--danger); color: #fff; font-size: 0.7rem; font-weight: 800; padding: 3px 8px; border-radius: 6px; }
  .product-card-swatches { display: flex; align-items: center; gap: 4px; padding: 10px 14px 0; flex-wrap: wrap; }
  .pc-swatch { width: 14px; height: 14px; border-radius: 50%; border: 1px solid rgba(0,0,0,0.12); flex-shrink: 0; }
  .pc-swatch-more { font-size: 0.68rem; font-weight: 700; color: var(--pdp-muted); margin-left: 2px; }
  .product-card-body { padding: 8px 14px 16px; display: flex; flex-direction: column; gap: 3px; }
  .product-card-price { display: flex; align-items: baseline; gap: 6px; }
  .product-card-price small { font-size: 0.7rem; color: var(--pdp-muted); }
  .product-card-price strong { font-size: 1.05rem; color: var(--success); font-weight: 800; }
  .product-card-price s { font-size: 0.75rem; color: var(--pdp-muted); }
  .product-card-vendor { font-size: 0.72rem; font-weight: 800; color: var(--pdp-muted); text-transform: uppercase; letter-spacing: 0.03em; }
  .product-card-title { font-size: 0.88rem; font-weight: 600; color: var(--color-text); line-height: 1.35; }

  .coll-pagination { display: flex; justify-content: center; gap: 8px; margin-top: 36px; }
  .coll-page-link { padding: 8px 14px; border: 1px solid var(--pdp-line); border-radius: 8px; font-size: 0.85rem; font-weight: 600; }
  .coll-page-link.is-active { background: var(--brand); border-color: var(--brand); color: #fff; }
  .coll-empty { padding: 60px 0; text-align: center; color: var(--pdp-muted); }

  @media (max-width: 900px) {
    .coll-body { grid-template-columns: 1fr; }
    .coll-sidebar { position: static; order: 2; }
    .product-grid { grid-template-columns: repeat(2, 1fr); }
    .coll-badges { grid-template-columns: 1fr; }
    .coll-count { margin-left: 0; }
  }
</style>

<script>
(function () {
  var sortSelect = document.querySelector('[data-coll-sort]');
  if (sortSelect) {
    sortSelect.addEventListener('change', function () {
      var url = new URL(window.location.href);
      url.searchParams.set('sort_by', this.value);
      window.location.href = url.toString();
    });
  }
  document.querySelectorAll('[data-filter-toggle]').forEach(function (btn) {
    btn.addEventListener('click', function () {
      var group = btn.closest('[data-filter-group]');
      if (!group) return;
      if (group.hasAttribute('data-collapsed')) group.removeAttribute('data-collapsed');
      else group.setAttribute('data-collapsed', '');
    });
  });

  // Client-side Cut + Color filtering (page-scoped; server-side facets aren't
  // available for these since products only carry one default variant).
  var cards = Array.prototype.slice.call(document.querySelectorAll('#collProductGrid .product-card'));
  var activeCuts = new Set();
  var activeColors = new Set();

  function applyFilters() {
    cards.forEach(function (card) {
      var cutOk = activeCuts.size === 0 || activeCuts.has(card.dataset.cut);
      var cardColors = (card.dataset.colors || '').split('|').filter(Boolean);
      var colorOk = activeColors.size === 0 || cardColors.some(function (c) { return activeColors.has(c); });
      card.classList.toggle('is-filtered-out', !(cutOk && colorOk));
    });
  }

  document.querySelectorAll('[data-cut-filter]').forEach(function (cb) {
    cb.addEventListener('change', function () {
      if (cb.checked) activeCuts.add(cb.value); else activeCuts.delete(cb.value);
      applyFilters();
    });
  });
  document.querySelectorAll('[data-color-filter]').forEach(function (btn) {
    btn.addEventListener('click', function () {
      var color = btn.dataset.color;
      btn.classList.toggle('is-active');
      if (btn.classList.contains('is-active')) activeColors.add(color); else activeColors.delete(color);
      applyFilters();
    });
  });
})();
</script>

{% schema %}
{
  "name": "T-Shirt Collection",
  "settings": [
    { "type": "checkbox", "id": "show_quick_nav", "label": "Show quick category nav", "default": true },
    { "type": "range", "id": "products_per_page", "label": "Products per page", "min": 12, "max": 48, "step": 4, "default": 24 },
    { "type": "text", "id": "delivery_note", "label": "Delivery note", "default": "Next-Day Delivery — Order by 5PM" }
  ],
  "blocks": [
    {
      "type": "badge",
      "name": "Info badge",
      "settings": [
        { "type": "text", "id": "text", "label": "Badge text", "default": "Free shipping on $59+" }
      ]
    },
    {
      "type": "quick_link",
      "name": "Quick nav link",
      "settings": [
        { "type": "image_picker", "id": "image", "label": "Thumbnail (optional)" },
        { "type": "text", "id": "label", "label": "Label", "default": "Short Sleeve" },
        { "type": "url", "id": "link", "label": "Link" }
      ]
    }
  ],
  "presets": [
    {
      "name": "T-Shirt Collection",
      "blocks": [
        { "type": "badge", "settings": { "text": "Next-Day Delivery" } },
        { "type": "badge", "settings": { "text": "Free Shipping on $59+" } },
        { "type": "badge", "settings": { "text": "No Minimums" } },
        { "type": "badge", "settings": { "text": "Bulk Discounts" } },
        { "type": "quick_link", "settings": { "label": "All T-Shirts", "link": "/collections/all" } },
        { "type": "quick_link", "settings": { "label": "Short Sleeve", "link": "/collections/short-sleeve" } },
        { "type": "quick_link", "settings": { "label": "Long Sleeve", "link": "/collections/long-sleeve" } },
        { "type": "quick_link", "settings": { "label": "Tank Tops", "link": "/collections/tank-tops" } },
        { "type": "quick_link", "settings": { "label": "Tie-Dye", "link": "/collections/tie-dye" } }
      ]
    }
  ]
}
{% endschema %}
